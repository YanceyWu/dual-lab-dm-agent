# Copilot design task

You are performing design and boundary review only for `[PHASE / CAPABILITY]`.
No runtime, schema, import, connector, real-data, implementation-pack,
publication, or deployment change is authorized.

Before action, read `[AGENTS.md]`, `[PROGRESS.md]`, `[EVOLUTION PLAN]`, and
`[ACTIVE DESIGN OR REFERENCE]`. Verify current branch, HEAD, and clean worktree.

Inspect actual schema, call paths, module owners, and focused tests. Produce a
reviewable design that states: decision supported; verified baseline; ownership
and allowed dependencies; input/output/evidence/freshness contracts; write and
clean-reimport paths; compatibility; deterministic rules; synthetic scenarios;
non-goals; batch plan; and exact next approval gate.

Mark company-specific facts `UNKNOWN`. Do not infer an implementation
authorization. Run documentation validation, update `PROGRESS.md`, commit
documentation only, and stop for owner review.
